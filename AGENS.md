# AGENS.md - 全栈UI/UX设计顾问指南

**项目背景：**
本项目是一个 **AI壁纸分享与社区平台 (AI Wallpaper Frontend)**。
- **项目类型**：图片素材分享与社交社区 Web 应用。
- **目标用户**：寻找高质量AI生成壁纸的普通用户、分享AI创作作品的创作者/设计师。
- **核心功能**：
    - **壁纸浏览与下载**：瀑布流展示、高清大图查看、分类检索。
    - **社区互动**：发布帖子、评论、点赞（喜欢）、收藏、关注创作者。
    - **个人中心**：用户资料管理、作品展示、收藏夹管理。
- **品牌定位**：现代、精致、视觉导向。界面设计应最大程度突显图片内容本身，UI 元素应起到支撑和引导作用，而不应喧宾夺主。

**设计需求：**
1. **核心设计原则**：
    - **低饱和度 (Low Saturation)**：减少刺眼纯色，使用高级灰度色彩（Apple Style Greys）。
    - **呼吸感暗色 (Breathing Dark)**：弃用纯黑，改用极深灰蓝 (`#0D0D0F`) 配合紫色弥散光，消除压抑感。
    - **通透感 (Translucency)**：毛玻璃 (`backdrop-filter`) + 内发光 (`Inner Glow`)，即使在暗色下也能感知层级。

2. **配色方案 (Zhefeng/Apple Style)**：
    - **亮色模式 (Light Mode - Pure & Paper)**：
        - 背景：`#F5F5F7` (带微蓝的白)。
        - 卡片：`#FFFFFF` (纯白)，无模糊，强调干净整洁。
        - 主文字：`#1D1D1F`。
        - 品牌色：Bright Blue (`#007AFF`)。
    - **暗色模式 (Dark Mode - Deep Space)**：
        - 背景：`#0D0D0F` (极深灰蓝) + 紫色弥散光 (`rgba(100, 70, 255, 0.05)`)。
        - 卡片：`#1C1C1E` (深灰层级)。
        - 主文字：`#F5F5F7` (柔白)。
        - 品牌色：Deep Purple (`#5E5CE6`)。
        - 光影细节：顶部边缘 1px 内发光 (`rgba(255,255,255,0.08)`)。

3. **UI 视觉升级**：
    - **大圆角 (Large Radius)**：卡片 `24px`，图片 `16px`。
    - **文字呼吸感**：全局字间距 `0.02em`，优化行高。
    - **按钮质感**：
        - Light: 经典蓝色 + 投影。
        - Dark: 深紫 + 内发光描边。

4. **响应式与可访问性**：
    - 保持 WCAG AA 对比度标准。
    - 移动端适配大圆角和触摸区域。

**当前进展：**
- **已有材料**：
    - 现有的 Vue 3 + Vite + Tailwind CSS + Element Plus 代码库。
    - **三层样式架构 (已更新 v3 - Breathing Dark)**：
        1. `tokens.css`: 哲风风格中性色、弥散光变量、内发光变量。
        2. `theme.css`: 亮暗色彩映射，暗色模式启用 Inner Glow。
        3. `element-bridge.css`: 卡片内发光实现、字体间距优化。
        4. `base.css`: 全局背景弥散光效实现。
- **已完成部分**：
    - 核心页面（首页、社区、详情页）的功能实现。
    - 基础的深色模式架构搭建。
    - 正在进行的“去硬编码”重构工作。
    - **API 模块重构 (v3.3)**：拆分 `wallpaper.js` 为 `user.js`, `community.js`, `request.js`，完成新版后端接口（壁纸投稿、图片交互、用户资料）的对接。
- **技术约束**：
    - **前端框架**：Vue 3 (Composition API)。
    - **UI 库**：Element Plus (必须通过 `:deep()` 或全局 Bridge 适配，不引入外部深色主题包)。
    - **样式工具**：Tailwind CSS (用于布局和原子样式) + CSS Variables (用于主题切换)。

**具体请求：**
作为设计顾问和开发者，在后续工作中请遵循：
1. **设计建议/组件优化**：
    - 遇到硬编码颜色（如 Hex 代码 `background: #fff` 或 `color: #000`）时，必须替换为 `var(--app-bg-*)` 或 `var(--app-text-*)` 语义变量。
    - 针对 Element Plus 组件（如 Input, Select, Dialog），使用 `element-bridge.css` 进行全局或局部统一样式修正，避免散乱的 Style 补丁。
2. **配色方案/字体配对**：
    - 严格遵循 `src/styles/theme.css` 中的语义定义。
    - 字体使用系统默认无衬线字体栈，确保跨平台清晰度。
3. **交互流程**：
    - 保持现有 Vue Router 流程。
    - 优化加载状态和空状态的视觉反馈。
4. **输出要求**：
    - **代码优先**：直接修改 `.vue` 文件或 `.css` 文件，而非仅提供文字建议。
    - **深色模式优先**：每次修改 UI，必须检查 Dark Mode 下的表现（特别是文字可见性、边框对比度）。
    - **组件化思维**：能够复用的样式应提取到公共 CSS 或组件中。

**输出要求：**
- **设计原理**：修改样式时，简要说明是为了解决“对比度不足”、“层级混乱”还是“一致性”问题。
- **可执行代码**：直接提供修改后的代码块或执行文件写入操作。
- **状态管理**：考虑 Hover、Active、Disabled 状态的视觉反馈，特别是在深色模式下的亮度变化。
- **兼容性**：确保 Element Plus 的弹窗（Teleport 到 body 的元素）也能正确继承主题变量。


**更改内容**：随代码内容的更迭，做到AGENS.md的更新